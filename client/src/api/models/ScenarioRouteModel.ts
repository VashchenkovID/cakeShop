export declare module ScenarioRouteModel {
  export interface RootObject {
    distance: number;
    pointA: number;
    pointB: number;
    refPoints: Array<[number, number]>;
    routeId: number;
    routeTitle: number;
  }
}
